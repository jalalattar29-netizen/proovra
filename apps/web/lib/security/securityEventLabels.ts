/**
 * Moved to @proovra/shared (security-event-labels.ts) so native renders a
 * security event in the same words. Re-exported for existing importers.
 */
export {
  humanizeEventKey,
  presentOutcome,
  presentSecurityEvent,
  type SecurityEventPresentation,
} from "@proovra/shared";
