# Verification OUTPUT, not source.
#
# findings.json and the screenshots are regenerated by
#   pnpm exec playwright test --config apps/web/e2e/admin-control-plane/playwright.config.ts
# and are large and machine-specific. The completion ledger cites them by path;
# it does not require them to be committed, and committing a screenshot set that
# nobody regenerates is how stale evidence starts looking like fresh evidence.
*
!.gitignore
