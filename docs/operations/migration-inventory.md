# Migration Inventory — Phase O Risk Audit

Generated by `services/api/scripts/full-migration-audit.mjs`. **Read-only audit** — no DB was contacted.

## Summary
- Migrations audited: **120**
- CRITICAL findings: **173**
- HIGH findings: **438**
- MEDIUM findings: **163**
- LOW findings: **0**

- Verdict SAFE: **32**
- Verdict RISKY: **42**
- Verdict UNSAFE: **46**

## Findings by kind
| Kind | Count |
| --- | --- |
| `CREATE_INDEX_NO_IF_NOT_EXISTS` | 242 |
| `ALTER_COLUMN_TYPE` | 128 |
| `CREATE_TABLE_IF_NOT_EXISTS` | 113 |
| `ALTER_TYPE` | 86 |
| `ENUM_ADD_VALUE` | 86 |
| `INDEX_COLUMN_RISK` | 40 |
| `ADD_COLUMN_NO_IF_NOT_EXISTS` | 24 |
| `CREATE_INDEX_GUARDED` | 16 |
| `ALTER_COLUMN_SET_DEFAULT` | 15 |
| `ALTER_TABLE_DROP_COLUMN` | 10 |
| `DROP_INDEX` | 6 |
| `SET_NOT_NULL_WITH_READINESS` | 4 |
| `SET_NOT_NULL_NO_READINESS` | 2 |
| `ALTER_TABLE_RENAME` | 1 |
| `DROP_TABLE` | 1 |

## UNSAFE migrations (require review before any production action)

| Migration | CRITICAL | HIGH | MEDIUM | LOW | Tables |
| --- | --- | --- | --- | --- | --- |
| `20260201013040_align_schema` | 3 | 0 | 0 | 0 |  |
| `20260201120000_update_evidence_signingkey` | 3 | 5 | 1 | 0 | evidence, signing_keys |
| `20260204190000_add_auth_billing` | 7 | 0 | 0 | 0 | cases, entitlements, evidence, payments, +4 |
| `20260205090000_phase2_invites_claim` | 2 | 4 | 0 | 0 | evidence, guest_identities, team_invites |
| `20260205093000_evidence_parts` | 1 | 0 | 0 | 0 | evidence_parts |
| `20260205100000_b2b_org_locking` | 1 | 0 | 0 | 0 | case_access, evidence, teams |
| `20260215095541_email_password_auth` | 1 | 2 | 0 | 0 | password_reset_tokens, users |
| `20260324120000_add_team_activity` | 1 | 0 | 0 | 0 | team_activities |
| `20260404180000_admin_audit_log` | 1 | 0 | 0 | 0 | admin_audit_logs, users |
| `20260407_add_legal_acceptance_and_cookie_consent` | 2 | 0 | 0 | 0 | cookie_consent_records, user_legal_acceptances |
| `20260418_report_snapshot_fields` | 8 | 0 | 0 | 0 | reports |
| `20260510160000_phase_a_b_forensic_hardening` | 2 | 8 | 0 | 0 | evidence |
| `20260510200000_phase_c_hardening` | 2 | 0 | 0 | 0 | evidence |
| `20260521100000_add_review_operations_phase13` | 2 | 34 | 0 | 0 | evidence_review_workflows |
| `20260523100000_add_governance_platform_phase14` | 2 | 16 | 0 | 0 | case_legal_holds, evidence, workspace_governance_policies |
| `20260524100000_add_intelligence_phase15` | 5 | 0 | 0 | 0 | evidence_entities, evidence_extracted_texts, evidence_intelligence_jobs, evidence_semantic_chunks, +1 |
| `20260525100000_add_collaboration_phase16` | 4 | 24 | 0 | 0 | discussion_mentions, discussion_messages, discussion_participants, discussion_threads |
| `20260526100000_add_identity_phase17` | 10 | 0 | 0 | 0 | access_reviews, external_identity_mappings, member_capability_grants, member_delegated_admin_scopes, +3 |
| `20260527100000_add_communications_phase18` | 3 | 0 | 0 | 0 | communication_messages, communication_preferences, verification_attempts, workflow_intake_sessions |
| `20260528100000_add_identity_security_phase19` | 4 | 0 | 0 | 0 | organization_security_policies, revoked_sessions, risk_signals, step_up_challenges, +1 |
| `20260529100000_add_operational_incidents_phase21` | 2 | 0 | 0 | 0 | operational_incident_events, operational_incidents |
| `20260530100000_add_workflow_engine_phase22` | 5 | 0 | 0 | 0 | evidence_workflow_instance_evidence, evidence_workflow_instances, evidence_workflow_step_instances, evidence_workflow_templates, +1 |
| `20260531100000_add_search_discovery_phase24` | 3 | 0 | 0 | 0 | evidence_search_documents, saved_search_views |
| `20260601100000_add_reviewer_operations_phase25` | 3 | 0 | 0 | 0 | evidence_review_workflows, review_escalations, reviewer_workload_snapshots |
| `20260602100000_phase25_5_reviewer_ops_hardening` | 3 | 0 | 0 | 0 | reviewer_ops_reminders, saved_search_views, workspace_governance_policies |
| `20260603100000_phase26_identity_governance` | 3 | 0 | 0 | 0 | authenticated_sessions, scim_provisioning_tokens, sso_connections |
| `20260604100000_phase26_5_identity_hardening` | 5 | 0 | 0 | 0 | authenticated_sessions, scim_groups, sso_callback_attempts, sso_connections |
| `20260605100000_phase26_75_identity_runtime` | 5 | 0 | 0 | 0 | authenticated_sessions, geo_intelligence_lookups, trusted_devices |
| `20260606100000_phase27_retention_lifecycle` | 5 | 0 | 0 | 0 | destruction_reviews, evidence, evidence_lifecycle_events, evidence_retention_policies, +1 |
| `20260613100000_phase27_5_governance_operationalization` | 5 | 0 | 0 | 0 | destruction_executions, governance_export_snapshots, governance_notifications, governance_reconciliation_runs, +1 |
| `20260620100000_phase24_31_consolidated_drift_patches` | 23 | 3 | 0 | 0 | evidence_ocr_text, evidence_part_derived_assets, evidence_part_exif_summaries, evidence_search_documents, +13 |
| `20260625100000_phase328cpppp_dashboard_intelligence_closure` | 2 | 0 | 0 | 0 | access_anomalies, evidence_annotations, evidence_integrity_snapshots, evidence_reviewer_comments |
| `20260626100000_phase328cppppp_structural_intelligence_closure` | 5 | 0 | 0 | 0 | case_comments, case_evidence_links, evidence_integrity_snapshots, operational_timeline_events, +2 |
| `20260627100000_phase328c_control_plane_closure` | 1 | 0 | 0 | 0 | operational_correlations, operational_incidents |
| `20260628100000_phase328c_workflow_causality` | 5 | 0 | 0 | 0 | operational_causality_chains, operational_causality_links, operational_workflow_actions, operational_workflow_events, +1 |
| `20260629100000_phase328c_enterprise_gap_closure` | 7 | 0 | 0 | 0 | bulk_operational_action_items, bulk_operational_action_runs, operational_graph_edges, operational_graph_nodes, +3 |
| `20260630100000_phase328d_matter_workspace` | 6 | 0 | 0 | 0 | case_assignments, case_risk_snapshots, case_status_history, cases |
| `20260720100000_personal_workspace_bootstrap` | 1 | 0 | 0 | 0 | teams |
| `20260720200000_dashboard_projections` | 2 | 0 | 0 | 0 | org_health_projections, reviewer_queue_projections |
| `20260721000000_workspace_persona_profile` | 1 | 0 | 0 | 0 | workspace_persona_profiles |
| `20260925000000_phase0_schema_catchup` | 10 | 40 | 126 | 0 | access_reviews, cases, demo_requests, destruction_reviews, +15 |
| `20260927000000_p2_7x_stage6_invite_token_hash` | 1 | 2 | 0 | 0 | organization_invites |
| `20260928000000_p2_7x_stage6_teams_org_not_null` | 1 | 0 | 0 | 0 |  |
| `20261001000000_phase_a1_evidence_org_tenancy` | 1 | 0 | 0 | 0 |  |
| `20261009000000_drop_reviewer_queue_projection` | 1 | 0 | 0 | 0 |  |
| `email_password_auth` | 1 | 2 | 0 | 0 | password_reset_tokens, users |

## RISKY migrations (idempotency / re-run gaps)

| Migration | CRITICAL | HIGH | MEDIUM | LOW | Tables |
| --- | --- | --- | --- | --- | --- |
| `20260131235343_init` | 0 | 16 | 0 | 0 | custody_events, evidence, reports, signing_keys |
| `20260201033040_fix_evidence_file_sha256_index` | 1 | 1 | 0 | 0 |  |
| `20260201133000_fix_evidence_file_sha256_index_v2` | 1 | 1 | 0 | 0 |  |
| `20260321000000_expand_user_country_field` | 0 | 1 | 1 | 0 | users |
| `20260321110000_add_evidence_archive_field` | 0 | 3 | 0 | 0 | evidence |
| `20260404193000_add_analytics_tables` | 0 | 7 | 0 | 0 | AnalyticsEvent, AnalyticsSession |
| `20260430_add_tsa_input_digest_fields` | 0 | 2 | 0 | 0 | evidence |
| `20260504150000_add_evidence_internal_notes` | 0 | 1 | 0 | 0 | evidence |
| `20260504170000_add_capture_plan_and_part_metadata` | 0 | 6 | 0 | 0 | evidence, evidence_parts |
| `20260508133000_add_evidence_operations_workspace_features` | 0 | 16 | 0 | 0 | evidence_ai_categorizations, evidence_annotations, evidence_legal_notes, evidence_reviewer_comments, +1 |
| `20260508153000_add_evidence_review_workflow_relationships_audit` | 0 | 19 | 0 | 0 | evidence_relationships, evidence_review_workflow_events, evidence_review_workflows, evidence_reviewer_audit_events |
| `20260510120000_add_capture_sessions` | 0 | 7 | 0 | 0 | capture_session_events, capture_sessions |
| `20260516120000_add_workflow_template_foundation` | 0 | 8 | 0 | 0 | evidence_workflow_templates, teams |
| `20260516180000_add_workflow_intake_links_phase4` | 0 | 20 | 0 | 0 | workflow_intake_links, workflow_intake_sessions |
| `20260516220000_add_evidence_requests_phase7` | 0 | 17 | 0 | 0 | evidence_request_deliverables, evidence_request_events, evidence_request_responses, evidence_requests |
| `20260517020000_add_notification_deliveries_phase8` | 0 | 7 | 0 | 0 | notification_deliveries |
| `20260517060000_add_notification_template_context` | 0 | 1 | 0 | 0 | notification_deliveries |
| `20260517100000_add_governance_phase9` | 0 | 15 | 0 | 0 | evidence_legal_holds, workspace_governance_policies |
| `20260517140000_add_integrations_phase10` | 0 | 10 | 0 | 0 | api_credentials, integration_webhook_deliveries, integration_webhook_endpoints, workspace_governance_policies |
| `20260518100000_add_integrations_phase10_5` | 0 | 3 | 0 | 0 | api_credential_usage_logs |
| `20260519100000_add_security_hardening_phase11` | 0 | 7 | 0 | 0 | file_security_scans, security_events |
| `20260520100000_add_reliability_phase12` | 0 | 3 | 0 | 0 | upload_sessions |
| `20260722000000_r8_1_mfa_activation` | 0 | 5 | 0 | 0 | mfa_factors, mfa_recovery_codes |
| `20260724000000_r8_1_3_mfa_pending_challenges` | 0 | 3 | 0 | 0 | mfa_pending_challenges |
| `20260725000000_r8_1_4_mfa_recovery_requests` | 0 | 5 | 0 | 0 | mfa_recovery_request_approvals, mfa_recovery_requests |
| `20260727000000_r8_1_6_recovery_digest_logs` | 0 | 2 | 0 | 0 | mfa_recovery_digest_logs |
| `20260728000000_r8_1_7_digest_preferences` | 0 | 4 | 0 | 0 | mfa_admin_digest_preferences, mfa_recovery_admin_digest_logs |
| `20260801000000_phase_e3_automation_foundation` | 0 | 7 | 0 | 0 | automation_rules, automation_runs |
| `20260802000000_phase_e3_2_webhook_delivery` | 0 | 6 | 0 | 0 | automation_webhook_deliveries, automation_webhook_destinations |
| `20260803000000_phase_e3_3_async_delivery_runtime` | 0 | 5 | 0 | 0 | automation_webhook_deliveries |
| `20260804000000_phase_e10_1_stripe_webhook_idempotency` | 0 | 3 | 0 | 0 | stripe_webhook_events |
| `20260926000000_p2_7x_stage1_org_model_additive` | 0 | 11 | 0 | 0 | organization_audit_events, organization_invites, organization_memberships, organization_policies, +2 |
| `20260929000000_phase_b2_workflow_review_decisions` | 0 | 4 | 0 | 0 | workflow_review_decisions |
| `20261003000000_phase_g3_1_notification_preferences` | 0 | 3 | 0 | 0 | workspace_notification_preferences |
| `20261004000000_phase_m3_1_siu_durability` | 0 | 18 | 0 | 0 | case_siu_checklist_items, case_siu_exports, case_siu_follow_ups, case_siu_profiles, +1 |
| `20261005000000_phase_m3_2_siu_governance_export` | 0 | 5 | 0 | 0 | case_siu_saved_views |
| `20261101000000_phase_3a_redaction_platform` | 0 | 3 | 0 | 0 | redaction_activity, redaction_approvals, redaction_decisions, redaction_derivatives, +4 |
| `20261201000000_phase_3a_elite_closure_policy_video` | 0 | 4 | 0 | 0 | redaction_policies, redaction_policy_assignments, redaction_policy_audit, redaction_policy_versions, +4 |
| `20261215000000_phase_3b_intelligence_platform` | 0 | 1 | 0 | 0 | media_intelligence_entities, media_intelligence_records, provider_budget_alerts, provider_budgets, +2 |
| `20261220000000_phase_4a_trust_and_governance` | 0 | 8 | 0 | 0 | access_review_campaigns, access_review_items, cross_org_review_grants, delegated_admin_grants, +12 |
| `20261225000000_phase_4a_enterprise_closure` | 0 | 2 | 2 | 0 | department_memberships, evidence, security_claim_checks, trust_center_articles |
| `20261230000000_phase_4b_packaging_and_lifecycle` | 0 | 4 | 0 | 0 | archive_tier_transitions, chain_transfers, destruction_certificates, destruction_requests, +8 |

## SAFE migrations

| Migration | CRITICAL | HIGH | MEDIUM | LOW | Tables |
| --- | --- | --- | --- | --- | --- |
| `20260201013225_custody_event_index` | 0 | 0 | 0 | 0 |  |
| `20260201120000_phase9_hardening` | 0 | 6 | 0 | 0 | evidence |
| `20260201124500_add_document_evidence_type` | 0 | 2 | 0 | 0 |  |
| `20260201132000_add_evidence_locked_event` | 0 | 2 | 0 | 0 |  |
| `20260312170000_add_evidence_tsa_fields` | 0 | 0 | 0 | 0 | evidence |
| `20260404200000_admin_audit_is_public` | 0 | 0 | 0 | 0 | admin_audit_logs |
| `20260404213000_analytics_admin_audit_hardening` | 0 | 0 | 1 | 0 | AnalyticsEvent, AnalyticsSession, admin_audit_logs |
| `20260417000000_create_verification_source_enum` | 0 | 0 | 0 | 0 | reports |
| `20260522100000_add_review_sla_defaults_phase13_5` | 0 | 0 | 0 | 0 | workspace_governance_policies |
| `20260620200000_reviewer_ops_naming_drift_repair` | 0 | 0 | 0 | 0 |  |
| `20260620300000_team_billing_naming_drift_repair` | 0 | 0 | 0 | 0 | teams |
| `20260726000000_r8_1_5_recovery_email_preflight` | 0 | 6 | 1 | 0 | mfa_recovery_requests |
| `20260930000000_phase_a0_integrity_hard_gate` | 0 | 4 | 0 | 0 |  |
| `20261002000000_phase_a2_pdf_artifact_status` | 0 | 4 | 0 | 0 | reports |
| `20261006000000_phase_o_final_production_column_repair` | 0 | 0 | 4 | 0 | discussion_mentions, evidence_saved_views, evidence_workflow_instances, upload_sessions |
| `20261007000000_phase_o_live_schema_compatibility_repair` | 0 | 0 | 0 | 0 | discussion_messages, discussion_participants, discussion_threads, evidence_entities, +13 |
| `20261008000000_phase_o_workflow_join_table_final_repair` | 0 | 0 | 4 | 0 | evidence_workflow_instance_evidence |
| `20261015000000_phase_2b_closure_invitation_delivery_sso` | 0 | 0 | 2 | 0 | external_review_invitation_deliveries, external_reviewer_role_assignments |
| `20261216000000_phase_3b_enterprise_closure` | 0 | 0 | 1 | 0 | intelligence_activity_events, reviewer_corrections |
| `20261231000000_phase_4b_final_closure` | 0 | 0 | 1 | 0 | archive_tier_transitions, destruction_certificates, evidence_exchange_package_builds |
| `20270101000000_phase_r3_model_catchup` | 0 | 0 | 0 | 0 | access_review_campaigns, access_review_items, capture_device_attestations, capture_trust_event_records, +32 |
| `20270102000000_phase_r7_schema_catchup` | 0 | 0 | 0 | 0 | access_review_campaigns, access_review_items, capture_device_attestations, capture_trust_event_records, +15 |
| `20270103000000_phase_r7_trust_schema_fix` | 0 | 0 | 9 | 0 | subprocessor_versions, subprocessors, trust_center_articles |
| `20270104000000_phase_r7_redaction_schema_fix` | 0 | 0 | 2 | 0 | redaction_decisions, redaction_derivatives, redaction_detections, redaction_policy_assignments, +4 |
| `20270105000000_phase_r7_reviewer_workspace_schema_fix` | 0 | 0 | 0 | 0 | coding_fields, coding_schemas, coding_values, qc_samples, +1 |
| `20270106000000_phase_r7_governance_schema_fix` | 0 | 0 | 7 | 0 | departments |
| `20270107000000_phase_r7_capture_trust_schema_fix` | 0 | 0 | 0 | 0 | capture_device_attestations, capture_trust_event_records |
| `20270108000000_phase_r7_intelligence_schema_fix` | 0 | 0 | 0 | 0 | provider_budgets |
| `20270109000000_phase_r7_redaction_policy_unique` | 0 | 0 | 1 | 0 |  |
| `20270201000000_phase_5_collaboration_teams` | 0 | 0 | 0 | 0 | collaboration_team_activity, collaboration_team_assignments, collaboration_team_invites, collaboration_team_members, +1 |
| `20270301000000_phase_7_collaboration_completion` | 0 | 0 | 0 | 0 | collaboration_team_access_review_items, collaboration_team_access_reviews, collaboration_team_comment_mentions, collaboration_team_comments, +3 |
| `20270401000000_phase_10_paypal_webhook_idempotency` | 0 | 0 | 0 | 0 | paypal_webhook_events |

## Detail — every finding above MEDIUM
### `20260131235343_init`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 88) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 91) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 94) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 97) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 100) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 103) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 106) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 109) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 112) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 115) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 118) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 121) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 124) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 127) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 130) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 133) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260201013040_align_schema`

- **CRITICAL** `ALTER_TABLE_DROP_COLUMN` (line 16) — DROP COLUMN is destructive and cannot be safely re-applied.
- **CRITICAL** `ALTER_TABLE_DROP_COLUMN` (line 22) — DROP COLUMN is destructive and cannot be safely re-applied.
- **CRITICAL** `DROP_INDEX` (line 13) — DROP INDEX risks production-read regressions.

### `20260201033040_fix_evidence_file_sha256_index`

- **CRITICAL** `DROP_INDEX` (line 3) — DROP INDEX risks production-read regressions.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 4) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260201120000_phase9_hardening`

- **HIGH** `ALTER_TYPE` (line 9) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 17) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 25) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 9) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 17) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 25) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.

### `20260201120000_update_evidence_signingkey`

- **CRITICAL** `ALTER_TABLE_DROP_COLUMN` (line 8) — DROP COLUMN is destructive and cannot be safely re-applied.
- **CRITICAL** `DROP_INDEX` (line 2) — DROP INDEX risks production-read regressions.
- **CRITICAL** `DROP_INDEX` (line 5) — DROP INDEX risks production-read regressions.
- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 10) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 11) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 15) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 18) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 21) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260201124500_add_document_evidence_type`

- **HIGH** `ALTER_TYPE` (line 9) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 9) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.

### `20260201132000_add_evidence_locked_event`

- **HIGH** `ALTER_TYPE` (line 9) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 9) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.

### `20260201133000_fix_evidence_file_sha256_index_v2`

- **CRITICAL** `DROP_INDEX` (line 3) — DROP INDEX risks production-read regressions.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 4) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260204190000_add_auth_billing`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 55) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 68) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 78) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 91) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 106) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 122) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 137) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260205090000_phase2_invites_claim`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 28) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 41) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **HIGH** `ALTER_TYPE` (line 9) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 21) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 9) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 21) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.

### `20260205093000_evidence_parts`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 1) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260205100000_b2b_org_locking`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 23) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260215095541_email_password_auth`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 19) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **HIGH** `ALTER_TYPE` (line 10) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 10) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.

### `20260321000000_expand_user_country_field`

- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 18) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.

### `20260321110000_add_evidence_archive_field`

- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 7) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 10) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 11) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260324120000_add_team_activity`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 2) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260404180000_admin_audit_log`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 5) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260404193000_add_analytics_tables`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 37) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 40) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 43) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 46) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 49) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 52) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 55) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260407_add_legal_acceptance_and_cookie_consent`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 1) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 25) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260418_report_snapshot_fields`

- **CRITICAL** `ALTER_TABLE_RENAME` (line 14) — RENAME breaks idempotency and Prisma client expectations during rolling deploys.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 51) — Index reports_content_structure_snapshot_idx ON reports(content_structure_snapshot) references column(s) {content_structure_snapshot} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 54) — Index reports_primary_content_kind_snapshot_idx ON reports(primary_content_kind_snapshot) references column(s) {primary_content_kind_snapshot} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 57) — Index reports_item_count_snapshot_idx ON reports(item_count_snapshot) references column(s) {item_count_snapshot} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 60) — Index reports_recorded_integrity_verified_at_utc_snapshot_idx ON reports(recorded_integrity_verified_at_utc_snapshot) references column(s) {recorded_integrity_verified_at_utc_snapshot} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 63) — Index reports_last_verified_at_utc_snapshot_idx ON reports(last_verified_at_utc_snapshot) references column(s) {last_verified_at_utc_snapshot} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 66) — Index reports_reviewer_summary_version_idx ON reports(reviewer_summary_version) references column(s) {reviewer_summary_version} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 69) — Index reports_verification_package_version_idx ON reports(verification_package_version) references column(s) {verification_package_version} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.

### `20260430_add_tsa_input_digest_fields`

- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 2) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 3) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.

### `20260504150000_add_evidence_internal_notes`

- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 2) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.

### `20260504170000_add_capture_plan_and_part_metadata`

- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 2) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 5) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 8) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 11) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 14) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 17) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.

### `20260508133000_add_evidence_operations_workspace_features`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 104) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 106) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 108) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 111) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 113) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 115) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 118) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 120) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 122) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 125) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 127) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 129) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 131) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 134) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 136) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 138) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260508153000_add_evidence_review_workflow_relationships_audit`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 140) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 142) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 144) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 146) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 148) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 150) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 152) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 155) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 157) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 159) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 161) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 164) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 166) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 168) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 170) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 172) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 175) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 177) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 179) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260510120000_add_capture_sessions`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 55) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 57) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 59) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 61) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 63) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 78) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 80) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260510160000_phase_a_b_forensic_hardening`

- **CRITICAL** `ALTER_TABLE_DROP_COLUMN` (line 9) — DROP COLUMN is destructive and cannot be safely re-applied.
- **CRITICAL** `DROP_INDEX` (line 10) — DROP INDEX risks production-read regressions.
- **HIGH** `ALTER_TYPE` (line 25) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 26) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 27) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 28) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 25) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 26) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 27) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 28) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.

### `20260510200000_phase_c_hardening`

- **CRITICAL** `ALTER_TABLE_DROP_COLUMN` (line 7) — DROP COLUMN is destructive and cannot be safely re-applied.
- **CRITICAL** `ALTER_TABLE_DROP_COLUMN` (line 8) — DROP COLUMN is destructive and cannot be safely re-applied.

### `20260516120000_add_workflow_template_foundation`

- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 41) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 43) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 81) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 83) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 85) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 87) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 97) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 101) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260516180000_add_workflow_intake_links_phase4`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 113) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 115) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 117) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 119) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 121) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 123) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 172) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 174) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 176) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 178) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 180) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 182) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `ALTER_TYPE` (line 50) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 51) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 52) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 54) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 50) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 51) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 52) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 54) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.

### `20260516220000_add_evidence_requests_phase7`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 135) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 137) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 139) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 141) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 143) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 145) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 147) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 149) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 151) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 180) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 182) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 218) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 220) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 222) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 224) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 244) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 246) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260517020000_add_notification_deliveries_phase8`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 86) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 88) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 90) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 92) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 94) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 96) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 98) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260517060000_add_notification_template_context`

- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 16) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.

### `20260517100000_add_governance_phase9`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 68) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 102) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 104) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 106) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 108) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `ALTER_TYPE` (line 34) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 35) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 36) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 37) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 38) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 34) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 35) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 36) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 37) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 38) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.

### `20260517140000_add_integrations_phase10`

- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 26) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 69) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 71) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 73) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 106) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 133) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 135) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 137) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 139) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 141) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260518100000_add_integrations_phase10_5`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 29) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 31) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 33) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260519100000_add_security_hardening_phase11`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 52) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 54) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 56) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 73) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 75) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 77) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 79) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260520100000_add_reliability_phase12`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 57) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 59) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 61) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260521100000_add_review_operations_phase13`

- **CRITICAL** `INDEX_COLUMN_RISK` (line 96) — Index evidence_review_workflows_sla_status_due_at_idx ON evidence_review_workflows(sla_status,due_at) references column(s) {sla_status,due_at} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 98) — Index evidence_review_workflows_escalation_level_status_idx ON evidence_review_workflows(escalation_level,status) references column(s) {escalation_level,status} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **HIGH** `ALTER_TYPE` (line 24) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 25) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 26) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 27) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 28) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 30) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 31) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 32) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 33) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 34) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 35) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 36) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 37) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 38) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 39) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 40) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 41) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 24) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 25) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 26) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 27) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 28) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 30) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 31) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 32) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 33) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 34) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 35) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 36) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 37) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 38) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 39) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 40) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 41) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.

### `20260523100000_add_governance_platform_phase14`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 93) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 79) — Index evidence_retention_until_utc_idx ON evidence(retention_until_utc) references column(s) {retention_until_utc} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **HIGH** `ALTER_TYPE` (line 54) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 55) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 56) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 57) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 58) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 59) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 60) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 61) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 54) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 55) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 56) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 57) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 58) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 59) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 60) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 61) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.

### `20260524100000_add_intelligence_phase15`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 75) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 101) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 128) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 148) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 169) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260525100000_add_collaboration_phase16`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 80) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 117) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 142) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 162) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **HIGH** `ALTER_TYPE` (line 30) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 31) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 32) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 33) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 34) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 35) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 36) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 37) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 38) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 39) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 40) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 41) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 30) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 31) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 32) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 33) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 34) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 35) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 36) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 37) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 38) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 39) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 40) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 41) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.

### `20260526100000_add_identity_phase17`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 184) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 222) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 260) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 282) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 326) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 140) — Index team_members_team_id_status_idx ON team_members(team_id,status) references column(s) {team_id} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 142) — Index team_members_access_expires_at_utc_idx ON team_members(access_expires_at_utc) references column(s) {access_expires_at_utc} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 158) — Index workflow_intake_sessions_last_seen_at_utc_idx ON workflow_intake_sessions(last_seen_at_utc) references column(s) {last_seen_at_utc} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 177) — Index api_credentials_expires_at_utc_idx ON api_credentials(expires_at_utc) references column(s) {expires_at_utc} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 179) — Index api_credentials_disabled_at_utc_idx ON api_credentials(disabled_at_utc) references column(s) {disabled_at_utc} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.

### `20260527100000_add_communications_phase18`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 90) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 158) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 188) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260528100000_add_identity_security_phase19`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 63) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 95) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 134) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 164) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260529100000_add_operational_incidents_phase21`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 53) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 107) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260530100000_add_workflow_engine_phase22`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 33) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 89) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 111) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 149) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 28) — Index evidence_workflow_templates_team_status_idx ON evidence_workflow_templates(team_id,status) references column(s) {team_id} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.

### `20260531100000_add_search_discovery_phase24`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 19) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 91) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 86) — Index evidence_search_documents_searchable_text_trgm_idx ON evidence_search_documents(to_tsvector,COALESCE) references column(s) {to_tsvector,COALESCE} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.

### `20260601100000_add_reviewer_operations_phase25`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 39) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 148) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 32) — Index evidence_review_workflows_completion_due_at_utc_idx ON evidence_review_workflows(completion_due_at_utc) references column(s) {completion_due_at_utc} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.

### `20260602100000_phase25_5_reviewer_ops_hardening`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 49) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 39) — Index saved_search_views_team_scope_idx ON saved_search_views(team_id,scope) references column(s) {team_id} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 42) — Index saved_search_views_team_scope_visibility_idx ON saved_search_views(team_id,scope,visibility) references column(s) {team_id,visibility} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.

### `20260603100000_phase26_identity_governance`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 20) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 78) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 127) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260604100000_phase26_5_identity_hardening`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 46) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 97) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 25) — Index sso_connections_outage_idx ON sso_connections(outage_detected_at_utc) references column(s) {outage_detected_at_utc} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 37) — Index authenticated_sessions_team_heartbeat_idx ON authenticated_sessions(team_id,last_heartbeat_at_utc) references column(s) {team_id} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 39) — Index authenticated_sessions_team_risk_idx ON authenticated_sessions(team_id,risk_score) references column(s) {team_id,risk_score} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.

### `20260605100000_phase26_75_identity_runtime`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 49) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 26) — Index authenticated_sessions_team_quarantined_idx ON authenticated_sessions(team_id,quarantined_at_utc) references column(s) {team_id} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 28) — Index authenticated_sessions_team_recomputed_idx ON authenticated_sessions(team_id,last_risk_recomputed_at_utc) references column(s) {team_id,last_risk_recomputed_at_utc} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 40) — Index trusted_devices_team_decay_idx ON trusted_devices(team_id,trust_score_decay) references column(s) {team_id} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 42) — Index trusted_devices_team_quarantined_idx ON trusted_devices(team_id,quarantined_at_utc) references column(s) {team_id,quarantined_at_utc} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.

### `20260606100000_phase27_retention_lifecycle`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 48) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 101) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 141) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 199) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 41) — Index evidence_retention_policy_version_idx ON evidence(retention_policy_version_id) references column(s) {retention_policy_version_id} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.

### `20260613100000_phase27_5_governance_operationalization`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 147) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 196) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 268) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 314) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 382) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260620100000_phase24_31_consolidated_drift_patches`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 93) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 165) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 592) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 795) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 858) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 956) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 1066) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 1178) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 1366) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 1499) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 1630) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 1688) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 1749) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 1947) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 2053) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 2112) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 531) — Index saved_search_views_team_scope_idx ON saved_search_views(team_id,scope) references column(s) {team_id,scope} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 534) — Index saved_search_views_team_scope_visibility_idx ON saved_search_views(team_id,scope,visibility) references column(s) {team_id,scope,visibility} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 822) — Index evidence_ocr_text_uk ON evidence_ocr_text(evidence_id,COALESCE) references column(s) {COALESCE} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 889) — Index evidence_transcript_segments_uk ON evidence_transcript_segments(evidence_id,COALESCE) references column(s) {COALESCE} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 1239) — Index media_intelligence_signals_evidence_material_type_uk ON media_intelligence_signals(evidence_id,COALESCE) references column(s) {COALESCE} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 1937) — Index evidence_review_workflows_assignment_due_at_utc_idx ON evidence_review_workflows(assignment_due_at_utc) references column(s) {assignment_due_at_utc} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 1940) — Index evidence_review_workflows_completion_due_at_utc_idx ON evidence_review_workflows(completion_due_at_utc) references column(s) {completion_due_at_utc} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 687) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 723) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 736) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260625100000_phase328cpppp_dashboard_intelligence_closure`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 56) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 133) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260626100000_phase328cppppp_structural_intelligence_closure`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 91) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 157) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 216) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 277) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 334) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260627100000_phase328c_control_plane_closure`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 69) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260628100000_phase328c_workflow_causality`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 171) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 240) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 269) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 295) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 339) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260629100000_phase328c_enterprise_gap_closure`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 99) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 134) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 175) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 204) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 232) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 257) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 283) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260630100000_phase328d_matter_workspace`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 111) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 148) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 175) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 98) — Index case_team_reference_number_uniq ON cases(team_id,reference_number) references column(s) {team_id,reference_number} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 102) — Index cases_team_id_status_idx ON cases(team_id,status) references column(s) {team_id} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.
- **CRITICAL** `INDEX_COLUMN_RISK` (line 104) — Index cases_team_id_priority_idx ON cases(team_id,priority) references column(s) {team_id,priority} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.

### `20260720100000_personal_workspace_bootstrap`

- **CRITICAL** `INDEX_COLUMN_RISK` (line 28) — Index teams_owner_personal_uniq ON teams(owner_user_id) references column(s) {owner_user_id} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.

### `20260720200000_dashboard_projections`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 7) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 26) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260721000000_workspace_persona_profile`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 6) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.

### `20260722000000_r8_1_mfa_activation`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 46) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 48) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 75) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 77) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 79) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260724000000_r8_1_3_mfa_pending_challenges`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 42) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 46) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 48) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260725000000_r8_1_4_mfa_recovery_requests`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 45) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 47) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 49) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 74) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 76) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260726000000_r8_1_5_recovery_email_preflight`

- **HIGH** `ALTER_TYPE` (line 17) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 18) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 19) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 17) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 18) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 19) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.

### `20260727000000_r8_1_6_recovery_digest_logs`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 18) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 21) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260728000000_r8_1_7_digest_preferences`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 29) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 31) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 58) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 60) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260801000000_phase_e3_automation_foundation`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 73) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 74) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 76) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 122) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 125) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 127) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 129) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260802000000_phase_e3_2_webhook_delivery`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 80) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 83) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 137) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 140) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 142) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 144) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260803000000_phase_e3_3_async_delivery_runtime`

- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 45) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 46) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 47) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 51) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 58) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260804000000_phase_e10_1_stripe_webhook_idempotency`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 36) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 38) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 39) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260925000000_phase0_schema_catchup`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 776) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 793) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 821) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 842) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 862) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 887) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **CRITICAL** `ALTER_TABLE_DROP_COLUMN` (line 460) — DROP COLUMN is destructive and cannot be safely re-applied.
- **CRITICAL** `ALTER_TABLE_DROP_COLUMN` (line 590) — DROP COLUMN is destructive and cannot be safely re-applied.
- **CRITICAL** `ALTER_TABLE_DROP_COLUMN` (line 617) — DROP COLUMN is destructive and cannot be safely re-applied.
- **CRITICAL** `ALTER_TABLE_DROP_COLUMN` (line 682) — DROP COLUMN is destructive and cannot be safely re-applied.
- **HIGH** `ALTER_TYPE` (line 40) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 41) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 42) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 43) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 44) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 45) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 46) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 47) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 48) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 49) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 50) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 51) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 52) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 53) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 54) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 55) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 56) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 57) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 58) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 59) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 40) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 41) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 42) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 43) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 44) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 45) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 46) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 47) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 48) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 49) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 50) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 51) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 52) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 53) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 54) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 55) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 56) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 57) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 58) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 59) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.

### `20260926000000_p2_7x_stage1_org_model_additive`

- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 41) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 120) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 123) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 126) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 129) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 132) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 135) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 138) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 141) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 144) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 147) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260927000000_p2_7x_stage6_invite_token_hash`

- **CRITICAL** `SET_NOT_NULL_NO_READINESS` (line 54) — SET NOT NULL without a readiness marker risks rejecting NULL rows from production.
- **HIGH** `ADD_COLUMN_NO_IF_NOT_EXISTS` (line 39) — ADD COLUMN without IF NOT EXISTS is not idempotent. Re-running the migration after a partial failure breaks.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 65) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260928000000_p2_7x_stage6_teams_org_not_null`

- **CRITICAL** `SET_NOT_NULL_NO_READINESS` (line 29) — SET NOT NULL without a readiness marker risks rejecting NULL rows from production.

### `20260929000000_phase_b2_workflow_review_decisions`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 73) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 76) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 79) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 82) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20260930000000_phase_a0_integrity_hard_gate`

- **HIGH** `ALTER_TYPE` (line 22) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 23) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 22) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 23) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.

### `20261001000000_phase_a1_evidence_org_tenancy`

- **CRITICAL** `INDEX_COLUMN_RISK` (line 128) — Index evidence_team_id_organization_id_idx ON evidence(team_id,organization_id) references column(s) {team_id,organization_id} not added or guarded by this migration. Same failure class as 'mentioned_user_id does not exist'.

### `20261002000000_phase_a2_pdf_artifact_status`

- **HIGH** `ALTER_TYPE` (line 43) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ALTER_TYPE` (line 44) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 43) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
- **HIGH** `ENUM_ADD_VALUE` (line 44) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.

### `20261003000000_phase_g3_1_notification_preferences`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 45) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 54) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 57) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20261004000000_phase_m3_1_siu_durability`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 45) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 48) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 50) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 52) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 54) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 56) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 81) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 83) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 85) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 117) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 119) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 121) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 150) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 152) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 154) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 190) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 192) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 194) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20261005000000_phase_m3_2_siu_governance_export`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 33) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 36) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 38) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 40) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 42) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20261009000000_drop_reviewer_queue_projection`

- **CRITICAL** `DROP_TABLE` (line 12) — DROP TABLE is destructive.

### `20261101000000_phase_3a_redaction_platform`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 43) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 86) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 258) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20261201000000_phase_3a_elite_closure_policy_video`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 33) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 69) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 158) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 217) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20261215000000_phase_3b_intelligence_platform`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 44) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20261220000000_phase_4a_trust_and_governance`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 55) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 97) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 136) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 166) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 185) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 307) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 384) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 418) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20261225000000_phase_4a_enterprise_closure`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 82) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 120) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `20261230000000_phase_4b_packaging_and_lifecycle`

- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 32) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 61) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 295) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.
- **HIGH** `CREATE_INDEX_NO_IF_NOT_EXISTS` (line 443) — CREATE INDEX without IF NOT EXISTS is not idempotent. Re-running fails on the second attempt.

### `email_password_auth`

- **CRITICAL** `CREATE_TABLE_IF_NOT_EXISTS` (line 19) — CREATE TABLE IF NOT EXISTS silently skips the entire block when the table already exists, hiding missed column evolution. This is the root cause of the Phase O-Final `discussion_mentions.team_id` failure.
- **HIGH** `ALTER_TYPE` (line 10) — ALTER TYPE on an enum requires consideration for in-flight transactions and dependent columns.
- **HIGH** `ENUM_ADD_VALUE` (line 10) — ALTER TYPE ... ADD VALUE cannot run inside a transaction in older PostgreSQL; verify deploy mode.
