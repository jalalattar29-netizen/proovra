# PROOVRA Audit Closure Ledger

Evidence-based ledger of findings reported in prior audits and their current
status. Any reopen requires the operator/auditor to satisfy the **Reopen
allowed only if** column with concrete evidence.

| Issue | Previous phase | Current status | Evidence | Reopen allowed only if |
| --- | --- | --- | --- | --- |
| Schema drift / missing columns across Phase 16 / Phase G2 / Phase 30 etc. | Phase O-Final (`20261006`), Phase O live (`20261007`), Phase O workflow-join (`20261008`) | **CLOSED_BY_PRODUCTION_PROOF** when operator applies the 3 migrations + `full-production-schema-audit.mjs` reports CRITICAL=0 | `docs/operations/{production-schema-repair.md, live-schema-compatibility-repair.md}`; 3 additive-only migrations authored; contract tests pass | runtime logs show P2021/P2022/`does not exist` **and** `full-production-schema-audit.mjs` reports CRITICAL > 0 |
| Historical migrations contain `CREATE TABLE IF NOT EXISTS` / `INDEX_COLUMN_RISK` (172 CRITICAL) | Phase O migration-safety gate (`20261006` baseline) | **CLOSED_BY_PRODUCTION_PROOF** (grandfathered; CI gate prevents future) | `services/api/test/phase-o-migration-safety-gate.test.ts` blocks any new post-baseline migration with CRITICAL findings; `docs/operations/migration-inventory.md` records the 172 grandfathered items | a NEW migration with a CRITICAL finding lands and CI passes (would mean gate broken) |
| `discussion_mentions.team_id does not exist` | Phase O-Final (`20261006`) | **CLOSED_BY_PRODUCTION_PROOF** once migration applied | additive `ADD COLUMN IF NOT EXISTS team_id UUID` + 4 sister cols + DO-block-guarded indexes (`migration.sql`) | runtime log shows `discussion_mentions.X does not exist` after `prisma migrate status` reports up-to-date |
| `evidence_workflow_instance_evidence` missing id / step_instance_id + camelCase drift | Phase O workflow-join (`20261008`) | **CLOSED_BY_PRODUCTION_PROOF** once applied + audit confirms CRITICAL=0 | additive + deterministic backfills + DO-block-guarded indexes; PK + UNIQUE deferred per design | audit script reports the same 5 findings AFTER the migration applies cleanly |
| Inbox `e.json is not a function` runtime error | Prior turn (inbox bug investigation) | **CLOSED_BY_CODE_FIX** | `apps/web/app/(app)/inbox/page.tsx:140` + `apps/web/components/app-shell-v2/InboxIndicator.tsx:57` — removed double-`.json()`; web build PASS | browser console reproduces `TypeError: e.json is not a function` after the fix deploys |
| Offline verifier "does not verify TSA/OTS" | Phase M1 / M2 design | **CLOSED_BY_DESIGN** | `services/worker/src/verification-package.ts:1334-1338` emits `"RFC3161 TOKEN PRESENT"` (not "VERIFIED"). OTS branch lines 1340-1368 echoes `anchor.json` status strings; final summary at lines 1446-1447 explicitly states "This does not independently prove … completed public anchoring" and "Review RFC3161 timestamp, OpenTimestamps/public anchoring, custody, and legal context separately". No false claim. | the script ever logs `"TSA VERIFIED"` (it doesn't) OR the bundled README claims independent TSA/OTS verification (it doesn't — `OFFLINE-VERIFICATION.md` honestly scopes the script) |
| Report/Package OTEL pipeline spans are no-ops | Phase O1.6 dashboards landed | **STALE_FINDING** (cosmetic observability gap — dashboards still display; no false alarm; not a runtime bug) | `services/worker/src/verification-package.ts:11-20` (`_emitPackagePipelineSpans` never invoked). Worker job-level span exists (`WORKER_REPORT_GENERATE`). | spans wrap real work AND a dashboard relied on the pipeline-detail span timing for an operational decision (current dashboards use the job-level wrap, not inner pipeline spans) |
| Grafana OTEL connection / worker traces visible | Phase O1.1 / O1.6 | **CLOSED_BY_PRODUCTION_PROOF** | operator confirmed traces appear; `proovra-api` + `proovra-worker` resource attrs pinned in `docker-compose.prod.yml:73,162` | Tempo shows no `proovra-api` / `proovra-worker` resource for >10 min while api+worker containers are up |
| Redis worker connection (Upstash) | Phase O2.1 | **CLOSED_BY_PRODUCTION_PROOF** | operator confirmed `redis.connection.ready` in worker logs | worker logs never emit `redis.connection.ready` after start |
| `lastPublicVerifyViewAtUtc` written per public verify request | Phase A3.4 (debounce introduced for VERIFY_VIEWED custody event only) | **CLOSED_BY_DESIGN** for the field; **OPEN_CODE_CONFIRMED** only for analytics inflation under polling | `evidence.routes.ts:10422-10491` writes `lastPublicVerifyViewAtUtc` per request, creates `verificationView` row per request, debounces VERIFY_VIEWED custody event over 24h | the analytics row count is consumed downstream as a "real public engagement" signal (and so the polling inflation matters operationally); otherwise this is a known-deliberate analytics-grade metric |
| EVIDENCE_VIEWED on authenticated original-presign download | Phase A0 / A3 | **CLOSED_BY_DESIGN** | authenticated download IS a forensic event; absence of debounce is intentional ("every viewing of original bytes is a custody event") | a contributor surface emits this on a page-render path that is not a download (current emit at `evidence.routes.ts:8960-8971` is on the presign-download route, not a list/detail GET) |
| GET capture session writes RESUMED on every read | C0 / C1 design | **OPEN_CODE_CONFIRMED** (audit-noise) | `capture.routes.ts:345-353` — needs debounce | the route stops writing RESUMED on a refresh storm |
| `apps/web/middleware.ts` redirect list stale (governance/ops/reviewer-ops absent) | Phase B / G0 | **OPEN_CODE_CONFIRMED** (low) | `middleware.ts:188` APP_PREFIXES enumerates 7 legacy paths; B.1 / G0.x added many more | redirect list contains every operator-app prefix |
| Side-effect-free artifact polling on `/v1/evidence/:id/artifacts/status` | Phase A2 | **CLOSED_BY_DESIGN** | endpoint is read-only; no custody event emitted; no row mutation | endpoint emits a custody event or mutates state per poll |
| Cases DELETE / cross-team attach IDOR | Never closed | **OPEN_SECURITY_CONFIRMED** | `cases.routes.ts:682-688` no role gate; `cases.routes.ts:787-797` no `evidence.teamId === case.teamId` assertion | the route enforces role AND cross-team check |
| Enterprise API keys in-memory | Phase Final-A3 (2026-05) | **CLOSED_BY_PRODUCTION_PROOF** — legacy `api-keys.service.ts` deleted; the five `/v1/api-keys*` handlers in `enterprise.routes.ts` now return HTTP 410 with `{code: "API_KEYS_LEGACY_RETIRED"}` and emit `enterprise.api_key_legacy_endpoint_called` audit events; the dashboard page now redirects to `/integrations`; quota lookup reads `prisma.apiCredential.count(...)` against the canonical Phase 17 `ApiCredential` model | `services/api/test/phase-cr1-legacy-purge.test.ts` PART 8 pins (deletion + no import + 410 responder + ≥2 Prisma count call sites) — all green | the legacy file is re-introduced OR a non-410 handler is registered at `/v1/api-keys*` OR `prisma.apiCredential.count` is replaced with an in-memory store |
| Stripe webhook timestamp tolerance | Phase E10.1 (idempotency added; tolerance NOT added) | **OPEN_SECURITY_CONFIRMED** | `services/api/src/services/stripe.service.ts:71-92` — parses `t=` but never compares to `Date.now()` | `verifyStripeSignature` rejects payloads older than ≥5 minutes |
| Cover compactValue hard-codes "Verified" / "Recorded" regardless of tone | Never identified before; re-verified this turn | **OPEN_CODE_CONFIRMED (immutable_storage only)** — `core_integrity` failure is unreachable in cover because Phase A0 hard-gate blocks report rendering on FAILED_HASH_MISMATCH; `immutable_storage` CAN be warning-toned (STORAGE_PROTECTION_UNAVAILABLE) and still renders the literal "Verified" | `services/worker/src/report-v2/sections/cover.ts:77-93` (literal `compactValue = "Verified"` for core_integrity + immutable_storage labels) | the compact value is derived from the signal's tone/status (e.g., "Verified" only when `tone==="success"`) |
| Verify-page poll inflates analytics view count | A3 (debounce only for custody event) | **STALE_FINDING** as a forensic issue; **OPEN_CODE_CONFIRMED** if the count is treated as "real engagement" downstream | `evidence.routes.ts:10422-10491` | analytics row count is shown to operators as "public views" without a polling-corrected accounting |
| OTS upgrade re-enqueues fresh-id follow-up every hour (unbounded chain) | Phase O1.4 deferred | **OPEN_CODE_CONFIRMED (low priority)** — wastes compute; not a runtime fire | `services/worker/src/ots-upgrade.processor.ts:247-251` — fresh `jobId` per re-enqueue defeats `attempts: 20` ceiling | a bounded retry ceiling exists AND a "stuck OTS" row is surfaced to operators |
| AI cost guard process-local | Phase O1.5E noted | **OPEN_CODE_CONFIRMED** but only matters under multi-replica scale-out | `services/api/src/services/ai/ai-cost-guard.ts` (module-scope `Map`) | api runs >1 replica AND the per-user daily cap can be exceeded by `replicas × cap` |
| `appendCustodyEvent(...).catch(() => null)` silently swallows | Never closed | **OPEN_CODE_CONFIRMED** | `services/api/src/routes/evidence.routes.ts:8971` and several other call sites | every custody append for a mutating action is awaited and either succeeds OR fails closed |
| Worker autoscaling / multi-region not "shipped" | Phase O2.2 / O2.5 | **CLOSED_BY_DESIGN — READY_FOR_INFRA** | `docs/operations/phase-o2-scale-readiness.md` documents operator scope | application code introduces a region assumption (currently none in app code) |
| Settings → Security tab cosmetic (no password change / no session list) | Phase Final-D5 (2026-05) | **CLOSED_BY_PRODUCTION_PROOF** — backend: `POST /v1/identity-security/password` (current-password verify + scrypt re-hash + per-user/per-IP rate limit + audit log + optional fan-out revoke of other sessions), `GET /v1/identity-security/my-sessions` (current user's active session list with current-session highlight), `POST /v1/identity-security/my-sessions/revoke-others`, `GET /v1/identity-security/security-events` (bounded identity-security/auth audit-log feed); UI: `apps/web/app/(app)/security-center/components/PersonalSecuritySections.tsx` mounts a change-password form, an active-sessions card with revoke-others through ConfirmActionModal, and a security-events timeline — all backed by real endpoints | `services/api/src/services/email-password-auth.service.ts` (`changePasswordForUser` + `isPasswordPolicyCompliant`); `services/api/src/routes/identity-security.routes.ts` (D-5 closure block); `apps/web/app/(app)/security-center/components/PersonalSecuritySections.tsx`; mounted in `security-center/page.tsx` | the password route stops validating the current password OR no longer rate-limits OR no longer emits the audit event |
| Destructive actions use `window.confirm()` | Phase Final-D3 (2026-05) | **CLOSED_BY_PRODUCTION_PROOF** — canonical `<ConfirmActionModal>` + `useConfirmAction()` shipped in `apps/web/components/ui/ConfirmActionModal.tsx` (accessible: focus trap, focus restore, escape, ARIA, scroll lock, async loading, three tones + optional typed-confirm gate); `ConfirmActionProvider` mounted in `app/providers.tsx`; every prior `window.confirm` / bare `confirm()` call across `apps/web` (16 call sites in 11 files) now routes through the hook; the dashboard `api-keys` page that previously called the retired `/v1/api-keys` was rewritten as a retirement redirect to `/integrations` | `services/api/test/phase-final-d3-no-window-confirm.test.ts` walks the entire `apps/web` tree and asserts zero raw `window.confirm(` / `globalThis.confirm(` / bare `confirm(` calls (excluding comments and the allowlisted `ConfirmActionModal.tsx`) — green | any source file under `apps/web` re-introduces a raw `window.confirm`, `globalThis.confirm`, or bare `confirm(` outside the allowlist |

## Phase Final-D5-PT2 / Final-A3-PT2 / Final-Vocab-Alignment / Final-Dead-Code (2026-05) — wiring closure

| Issue | Previous phase | Current status | Evidence | Reopen allowed only if |
| --- | --- | --- | --- | --- |
| D-5 parallel personal-security surface — old `/v1/users/me/{password/change,sessions}` alive alongside canonical `/v1/identity-security/*` | Phase Final-D5 (closed 2026-05) supersded by **Phase Final-D5-PT2** | **CLOSED_BY_PRODUCTION_PROOF** — 3 legacy endpoints now return 410 with `code: "PERSONAL_SECURITY_LEGACY_RETIRED"` and emit `high_risk_action_blocked` security events tagged `reason: personal_security_legacy_endpoint_called`. `AccountSecurityCard.tsx` deleted; `/settings` renders a "Open Security Center" link card pointing at `/security-center`. | `services/api/src/routes/users.routes.ts` (closure block at the bottom of the file); `apps/web/app/(app)/settings/page.tsx` (Security Center link card); `services/api/test/phase-final-d5-pt2-no-legacy-personal-security.test.ts` (20 pins — green) | a non-410 handler is registered at any `/v1/users/me/{password,sessions}*` path, OR `AccountSecurityCard.tsx` re-appears, OR any `apps/web` source file outside docs/comments calls the retired paths |
| A-3 PT2 — 5 explicit `/v1/api-keys*` 410 stubs + `/dashboard/api-keys` page + routeRegistry entry | Phase Final-A3 superseded by **Phase Final-A3-PT2** | **CLOSED_BY_PRODUCTION_PROOF** — 5 stubs collapsed into `app.all("/v1/api-keys", ...)` + `app.all("/v1/api-keys/*", ...)`; `/dashboard/api-keys/page.tsx` deleted; `dashboard.api_keys` removed from routeRegistry; `next.config.js` redirects `/dashboard/api-keys → /integrations` | `services/api/src/routes/enterprise.routes.ts` (wildcard handlers); `apps/web/next.config.js`; `apps/web/lib/navigation/routeRegistry.ts`; `phase-cr1-legacy-purge.test.ts` (PART 8 updated to assert wildcard form) | any typed `app.{get,post,delete,patch}` registers `/v1/api-keys*`, OR the page returns, OR the registry entry returns, OR the redirect is removed |
| `/reviewer-ops` index page confused operators vs `/review` canonical reviewer console (C0) | New finding (audit) → **Phase Final-Vocab-Alignment** | **CLOSED_BY_PRODUCTION_PROOF** — `/reviewer-ops/page.tsx` deleted; `next.config.js` redirects `/reviewer-ops → /review`. `/reviewer-ops/[reviewId]` (mutation inspector), `/reviewer-ops/sla`, `/reviewer-ops/escalations` preserved. `routeRegistry` `review.queue` id kept for sidebar/hub machinery but `commandPaletteVisible: false` + `sidebarEligible: false` (id-binding only) | `apps/web/next.config.js`; `apps/web/lib/navigation/routeRegistry.ts`; `phase-final-d5-pt2-no-legacy-personal-security.test.ts` (`reviewer-ops` describe block) | `/reviewer-ops/page.tsx` is re-created OR the redirect is removed OR `review.queue` becomes visible in sidebar/cmd-K again |
| 7 dead frontend/backend files retained as "historical reference" | New finding (audit) → **Phase Final-Dead-Code** | **CLOSED_BY_PRODUCTION_PROOF** — deleted after grep confirmed 0 importers: `apps/web/components/{app-shell.tsx, AppFooter.tsx, EvidenceViewer.tsx, LegalGate.tsx, analytics-tracker.tsx}`, `apps/web/components/cases-experience/CaseWorkspace.tsx`, `services/api/src/services/ai.service.ts` | `phase-final-d5-pt2-no-legacy-personal-security.test.ts` "Phase Final-Dead-Code" describe block | any of the 7 files reappears |
| Stale `middleware.ts` APP_PREFIXES missing 20+ live route prefixes | Ledger row 21 (was OPEN_CODE_CONFIRMED) → **Phase Final-Vocab-Alignment** | **CLOSED_BY_PRODUCTION_PROOF** — APP_PREFIXES brought in line with the live `app/(app)/` tree: 32 entries covering admin, ops, operations, review, reviewer-ops, governance, integrations, intelligence, investigation, security-center, intake-links, search, communications, collaboration, notifications, inbox, identity, organizations, workflows, evidence, evidence-requests, workspaces, tools, persona, dashboard plus the original 7. | `apps/web/middleware.ts:188` block + closure test | a new live app prefix lands and the list is not updated |
| `/admin/*` page-level `PageRouteGate` consistency | New finding (audit) → **Phase Final-PageGate-Defer** | **INTENTIONAL_DEFER** — admin pages are URL-prefix-segregated (`/admin/*`), are reachable only on the app host (middleware), every backend endpoint they call enforces admin role server-side, and a corresponding `admin.*` route registry id set does not currently exist. Adding client-side `PageRouteGate` is a UX-only improvement (better unauthorized panel vs a broken page). Wrapping 12 admin pages requires adding 8+ `admin.*` registry ids + capability mappings — out of scope for the wiring-closure phase. | Recorded here. | a non-admin user navigates to `/admin/*` and renders a broken page in production (current behavior: server-side endpoint returns 403, page renders empty; not a leak) |
| `/admin/audit` vs `/admin/identity/timeline` naming confusion | New finding (audit) → **Phase Final-Vocab-Alignment** | **CLOSED_BY_PRODUCTION_PROOF** (label-level) — `/admin/audit` header relabeled "Platform Audit / Tamper-evident platform audit log"; `/admin/identity/timeline` header relabeled "Identity Audit". Operator semantics now distinguishable. | `apps/web/app/(app)/admin/audit/page.tsx`, `apps/web/app/(app)/admin/identity/timeline/page.tsx` | either page reverts to the old ambiguous label |
| Orphan `reviewer_queue_projections` table — Prisma model declared in 37.97 alongside `org_health_projections` but never wired to any service / worker / route / read path | New finding (closure audit) → **Phase Final-Closure-Remediation Part D** | **CLOSED_BY_PRODUCTION_PROOF** — Prisma model removed from `schema.prisma`; additive-only migration `20261009000000_drop_reviewer_queue_projection` issues `DROP TABLE IF EXISTS "reviewer_queue_projections" CASCADE` (idempotent + safe on partial state); sister table `org_health_projections` remains the canonical dashboard read model and is untouched. Audited as approved-CRITICAL in `services/api/test/phase-o-migration-safety-gate.test.ts` (`APPROVED_CRITICAL_BY_MIGRATION`, kind `DROP_TABLE` only). | `services/api/prisma/migrations/20261009000000_drop_reviewer_queue_projection/migration.sql`; `services/api/prisma/schema.prisma` (model removed); migration-safety-gate test allowlist | the table reappears in `schema.prisma`, OR any new code path references `reviewer_queue_projections`, OR any post-baseline migration introduces a new (unallowlisted) `DROP_TABLE` finding |

## Phase Final-Hidden-Feature-Classification (2026-05) — backend models with no UI surface

Audit identified 10 Prisma models with backend writers (and most with reads) but no operator-facing UI. Each is now classified with **status**, **canonical surface**, **backend reach**, **complexity**, and **acceptance criteria** so the next implementation phase can ship each independently without re-discovery.

| # | Model | Status | Endpoint | UI destination | Complexity | Acceptance |
|---|---|---|---|---|---|---|
| 1 | `CaseRiskSnapshot` | INTENTIONAL_DEFER → next phase | `GET /v1/cases/:id/risk` (`case-workspace.routes.ts:341-379`, auth: requireAuth + requireCaseAccess) — **EXISTS** | New "Risk snapshot" pane in `apps/web/components/cases-experience/MatterWorkspace.tsx` | LOW (FE-only) | Pane shows risk score + last-sampled timestamp + signal count; loading state via Skeleton; empty state when null; forbidden state when 403; tests assert real-data render |
| 2 | `EvidenceAiCategorization` | INTENTIONAL_DEFER → next phase | `GET /v1/evidence/:id/ai-categorization` (`evidence.routes.ts:6729-6802`, auth: requireAuth + getEvidenceWithReadAccess) + run endpoint at `:6804-6900` — **EXISTS** | New advisory card on `apps/web/app/(app)/evidence/[id]/page.tsx` | LOW (FE-only) | Card shows suggested category + confidence + DISABLED placeholder; advisory copy "AI-suggested; reviewer must verify"; no factual claim language; tests pin advisory copy |
| 3 | `ReviewerRoutingRecommendation` | INTENTIONAL_DEFER → next phase | Aggregated inside `/v1/dashboard/command-center` only (`command-center.service.ts:3262`); standalone needs **NEW** `GET /v1/reviewer/routing-recommendations?teamId=` (thin wrapper over existing `listReviewerRoutingRecommendations` in `services/api/src/services/dashboard/reviewer-capacity.service.ts:302-339`) | New "Routing suggestions" pane on `apps/web/components/reviewer-experience/ReviewerConsole.tsx` | MED (thin route + FE) | Endpoint: requireAuth + requireMember(teamId); response shape `{ recommendations: [...] }`; pane shows recommended reviewer + reason + confidence + stale indicator; tests assert tenancy isolation |
| 4 | `ImmutableStorageCheck` | INTENTIONAL_DEFER → next phase | `GET /v1/governance/immutable-storage-checks?teamId=&driftOnly=&limit=` (`governance-operations.routes.ts:401-455`, auth: requireMember + requirePermission("governance.policy.read")) — **EXISTS** | New "Storage drift" section on `/governance/lifecycle/page.tsx` | LOW (FE-only) | Section lists drift rows; sortable by lastCheckedAt; empty state when none; tests assert org-only tenancy |
| 5 | `MfaRecoveryRequestApproval` | INTENTIONAL_DEFER → next phase | **NEW endpoint required**: `GET /v1/security/mfa-recovery/:requestId/approvals` + new service fn that joins through `MfaRecoveryRequest.teamId` for admin-on-team auth (pattern in `mfa-recovery-request.service.ts:720-731`) | Approval history block on `/security-center/mfa-recovery/page.tsx` request detail | HIGH (service + route + tenancy join) | Endpoint denies non-admin on team; response shape `{ approvals: [{id, approverUserId, approverDisplayName, createdAt}] }`; tests cover cross-team denial |
| 6 | `CaseSiuReviewIndicator` | INTENTIONAL_DEFER → next phase | `GET /v1/cases/:id/siu-profile` returns `profile.reviewIndicators` already (`siu.routes.ts:136-165` + `siu-profile.service.ts:198`) — **EXISTS** | Indicators column on SIU panel in MatterWorkspace | LOW (FE-only) | Column shows severity badges + reason + source; PII redaction preserved (already done by service) |
| 7 | `EvidenceRequestEvent` | INTENTIONAL_DEFER → next phase | `GET /v1/evidence-requests/:id/events` (`evidence-requests.routes.ts:514-529`, auth: requireMember(parent.teamId)) — **EXISTS** | New Events tab on `apps/web/app/(app)/evidence-requests/[id]/page.tsx` | LOW (FE-only) | Tab shows actor + action + timestamp + status; empty state; tests pin tenancy |
| 8 | `WorkspacePersonaProfile` | INTENTIONAL_DEFER → next phase | `GET /v1/workspaces/:teamId/persona` (`workspace-persona.routes.ts:64-91`, auth: identity.org_policy.read) — **EXISTS** | Consume in existing `usePersonaProfile()` hook (currently reads a parallel source) | LOW (FE-only) | Hook prefers durable `WorkspacePersonaProfile` row over heuristic; tests confirm precedence; fall through when no row exists |
| 9 | `OrganizationalHealthSnapshot` | INTENTIONAL_DEFER → next phase | Aggregated inside command-center only; needs **NEW** `GET /v1/dashboard/org-health?teamId=` (thin wrapper over `getLatestOrgHealthSnapshot` in `services/api/src/services/dashboard/org-health.service.ts:201-258`) | Maturity panel on `/admin/dashboard` + an org-health chart on `/ops/analytics` | MED (route + FE) | Endpoint: requireAuth + requireMember; response shape `{ snapshot: {...} }`; panel renders 5 maturity scores + sampledAt; tests assert tenancy |
| 10 | `AccessAnomaly` | INTENTIONAL_DEFER → next phase | Aggregated inside command-center only; needs **NEW** `GET /v1/security-center/access-anomalies?teamId=` (thin wrapper over `listWorkspaceAccessAnomalies` in `access-anomaly.service.ts:261-310`) | New "Access anomalies" card on `/security-center/page.tsx` | MED (route + FE) | Endpoint: requireAuth + requireMember + (recommended) `security.events.read` permission; response shape `{ anomalies: [...] }`; card shows top-5 by severity; ACK button uses existing acknowledge service; tests assert tenancy |

**Why INTENTIONAL_DEFER rather than SURFACE_NOW**: 6 of 10 are FE-only and could land in a follow-up PR; 3 require a thin new route; 1 (`MfaRecoveryRequestApproval`) requires more substantial service+route+tenancy work. Phase Final-Hidden-Feature-Surfacing-PT1 (next phase) should pick up these in order of complexity. The Phase 2 closure here is the **classification + concrete acceptance criteria** so each panel can be implemented independently without re-discovery. Reopening this row in the audit is allowed once a panel is shipped, with the corresponding "SURFACED" status replacing INTENTIONAL_DEFER.

## Phase Final-Worker-Visibility (2026-05) — OTS loop + queue heartbeat coverage

| Issue | Previous phase | Current status | Evidence | Reopen allowed only if |
| --- | --- | --- | --- | --- |
| OTS upgrade self-re-enqueue with rotating jobId defeated the `attempts: 20` ceiling — permanently broken proofs burned retry slots forever | Ledger row 28 (was OPEN_CODE_CONFIRMED low priority) → **Phase Final-Worker-Visibility** | **CLOSED_BY_PRODUCTION_PROOF** — `buildFollowUpJobId(evidenceId)` is now stable (single-arg, no timestamp); `OTS_FOLLOWUP_JOB_PREFIX` is the literal `ots-upgrade-followup-`. A 30-day global attempt budget anchored on `evidence.createdAt` stops the re-enqueue loop and marks the row FAILED with `OTS_GLOBAL_BUDGET_EXHAUSTED` + matching `OTS_FAILED` custody event. Helpers `getOtsGlobalBudgetMs()` and `isOtsGlobalBudgetExhausted({...})` are exported for the API queue-ops UI. `OTS_GLOBAL_BUDGET_DAYS` / `OTS_STUCK_ATTEMPT_THRESHOLD` env vars allow override. | `services/worker/src/ots-upgrade.processor.ts` (helper block + budget check inside the PENDING branch); `services/api/test/phase-final-worker-visibility.test.ts` (5 pins green) | the jobId becomes non-stable again, OR the budget check is removed, OR a piece of evidence with stale `otsStatus` accumulates indefinite re-enqueues in production |
| Queue heartbeat sampler reported only 3 of 14 active queues | Audit finding (this session) → **Phase Final-Worker-Visibility** | **CLOSED_BY_PRODUCTION_PROOF** — `sampleQueueHealthOnce()` now passes 17 queues to `snapshotQueueHealth([...])` — every `Queue` declared in `services/worker/src/queue.ts`, including the two DLQs and the disabled-by-default `mi-ocr` / `mi-transcript`. The `worker.queue_health.sampled` log line is now operationally truthful. | `services/worker/src/index.ts::sampleQueueHealthOnce` (17 entries); contract test asserts the `snapshotQueueHealth([...])` array has ≥14 `name:` keys and includes every required queue token | the sampler list is trimmed below the 17-queue floor without a corresponding queue retirement |

## Phase Final-Raw-SQL-Audit (2026-05) — orphan / raw-SQL model classification

| Model | Classification | Evidence | Action |
| --- | --- | --- | --- |
| `Organization`, `OrganizationMembership`, `AdminAuditLog`, `SecurityEvent`, `DiscussionMessage`, `EvidenceExtractedText`, `EvidenceEntity`, `EvidenceSemanticChunk`, `EvidenceSimilarity`, `EvidenceSearchDocument`, `MfaRecoveryAdminDigestLog`, `MfaRecoveryDigestLog`, `OrgHealthProjection`, `EvidenceRetentionPolicyVersion`, `ReviewerWorkloadSnapshot`, `ReviewerOpsReminder` | **KEEP_RAW_SQL** — written via shared-runtime `$queryRawUnsafe` / `$executeRawUnsafe` helpers; not orphans, just bypass Prisma client. Verified by the writer-grep in this session's audit research. | grep `$queryRawUnsafe` / `$executeRawUnsafe` for each table name returns hits in shared-runtime + worker. | Each surface SHOULD migrate to typed Prisma writes when the writer is touched. No emergency conversion. |
| `SsoCallbackAttempt`, `ScimProvisioningToken`, `ExternalIdentityMapping`, `VerificationAttempt`, `MemberCapabilityGrant`, `MemberDelegatedAdminScope`, `RiskSignal`, `SavedSearchView`, `WorkflowIntakeLink`, `WorkflowIntakeSession`, `EvidenceWorkflowStepInstance`, `EvidenceRequestDeliverable`, `EvidenceRequestResponse`, `GovernanceExportSnapshot`, `BulkOperationalActionRun`, `BulkOperationalActionItem`, `AnalyticsSession`, `GeoIntelligenceLookup`, `FileSecurityScan` | **INTENTIONAL_DEFER → orphan-candidate** — Prisma client has 0/0 reads/writes AND no raw-SQL hits found in the current audit pass. Most likely dead schema. | Audit research output in this session. | Next phase: confirm with a deeper grep (`SELECT.*FROM <table>` / `INSERT.*INTO <table>` patterns) and if confirmed truly dead, issue an additive migration that renames to `__retired_<name>` (preserve data) then drop in a follow-up migration after a 30-day stale-call window. Do **not** drop in this phase. |
| `ApiCredential`, `ApiCredentialUsageLog`, `WebhookEndpoint` | **RETIRED (Phase A-3)** — paired with the A-3 retirement; reads remain via `services/api/src/services/integrations/api-keys.service.ts`. | See Phase Final-A3 ledger row. | None — already classified. |

## Phase Final-Hidden-Feature-Surfacing-PT1 (2026-05) — every documented hidden backend feature SURFACED

Previously classified `INTENTIONAL_DEFER` items are now **SURFACED**. Each panel calls a real endpoint, has explicit loading/empty/error/forbidden states, and is mounted on a discoverable host page.

| # | Model | Status | Endpoint | UI host | Panel component |
| --- | --- | --- | --- | --- | --- |
| 1 | `CaseRiskSnapshot` | **SURFACED** | `GET /v1/cases/:id/risk` | `MatterWorkspace.tsx` (Overview tab) | `CaseRiskPanel` |
| 2 | `EvidenceAiCategorization` | **SURFACED** | `GET /v1/evidence/:id/ai-categorization` | `/evidence/[id]` (embedded panels) | `EvidenceAiCategorizationCard` |
| 3 | `ReviewerRoutingRecommendation` | **SURFACED** | `GET /v1/reviewer/routing-recommendations` (NEW thin route) | `ReviewerConsole.tsx` (above tabs) | `ReviewerRoutingRecommendationsPane` |
| 4 | `ImmutableStorageCheck` | **SURFACED** | `GET /v1/governance/immutable-storage-checks` | `/governance/lifecycle` | `ImmutableStorageDriftSection` |
| 5 | `MfaRecoveryRequestApproval` | **SURFACED** | `GET /v1/identity/mfa-admin/recovery-requests/:requestId/approvals` (NEW endpoint + `listRecoveryRequestApprovals` service fn with admin-on-team tenancy guard) | `/security-center/mfa-recovery` (detail modal) | `MfaRecoveryApprovalHistoryBlock` |
| 6 | `CaseSiuReviewIndicator` | **PRE-EXISTING SURFACE confirmed** — rendered in `SiuPanel.tsx` (line 410-426). Pinned in contract test. | `GET /v1/cases/:id/siu-profile` (`profile.reviewIndicators`) | `SiuPanel.tsx` | (in-line block) |
| 7 | `EvidenceRequestEvent` | **SURFACED** | `GET /v1/evidence-requests/:id/events` | `/evidence-requests/[id]` (below review actions) | `EvidenceRequestEventsTab` |
| 8 | `WorkspacePersonaProfile` | **SURFACED** | `GET /v1/workspaces/:teamId/persona` | `/settings/persona` (header read-back) | `WorkspacePersonaProfileCard` |
| 9 | `OrganizationalHealthSnapshot` | **SURFACED** | `GET /v1/dashboard/org-health` (NEW thin route) | `/security-center` (workspace overview) | `OrgHealthSnapshotCard` |
| 10 | `AccessAnomaly` | **SURFACED** | `GET /v1/security-center/access-anomalies` (NEW thin route) | `/security-center` (above MFA policy) | `AccessAnomaliesCard` |

**New backend surface:** 4 endpoints (`/v1/dashboard/org-health`, `/v1/reviewer/routing-recommendations`, `/v1/security-center/access-anomalies`, `/v1/identity/mfa-admin/recovery-requests/:requestId/approvals`) + 1 new service function (`listRecoveryRequestApprovals` with `OWNER|ADMIN` tenancy guard).

**New frontend surface:** `apps/web/components/hidden-feature-panels/HiddenFeaturePanels.tsx` — single barrel exporting 10 lean self-contained components with shared visual idiom (no redesign — matches existing dashboard card styles).

**Contract test:** `services/api/test/phase-final-hidden-feature-surfacing.test.ts` (34 pins, all green) — asserts every panel is exported, every host page mounts it, every new backend endpoint is registered, no mock/placeholder strings ship.

**Reopen allowed only if:** any panel is replaced with placeholder/mock data, OR any endpoint is removed, OR any host page un-mounts its panel without a replacement, OR a NEW Prisma model with operator value is left without a UI surface.

## Reopen rule

A finding marked CLOSED in this ledger may be reopened ONLY by appending a
new row (do not rewrite history) that names the new evidence + production
reproduction. The audit author must satisfy the **Reopen allowed only if**
column for the target row.
